# Archive

This folder keeps the original custom integration prototype that existed before the repository was converted into a standalone Home Assistant add-on repository.

The archived files are kept for reference only. The active add-on now lives in `solid_sync/`.

